package webservice_layer;

public class Task {
	public int id;
	public String name;
	public int duration;
	public int start;
	
	public String toString(){
		return "Task: " + id + ";" + id + ";" + name + ";" + duration + ";" + start;
	}	
}
